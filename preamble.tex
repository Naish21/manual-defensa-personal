% =====================================================================
%  preamble.tex
%  Configuración global del documento: paquetes, idioma, estilo,
%  cajas destacadas y bibliografía.
%
%  Motor recomendado: pdflatex (compila también con xelatex/lualatex).
%  Bibliografía: BibTeX + natbib (estilo autor-año).
% =====================================================================

% ---------------------------------------------------------------------
%  Codificación y tipografía
% ---------------------------------------------------------------------
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

% Tipografía profesional: Linux Libertine (serif) + Biolinum (sans) si están
% disponibles; degradación elegante a Latin Modern.
\IfFileExists{libertine.sty}{%
  \usepackage{libertine}%
}{%
  \IfFileExists{lmodern.sty}{\usepackage{lmodern}}{}%
}

\usepackage[
  final,
  protrusion=true,
  expansion=false,
  tracking=true,
  kerning=true,
  spacing=true
]{microtype}
\usepackage{textcomp}         % Símbolos adicionales

% Iconos de las cajas destacadas (con degradación segura si no está el paquete)
\IfFileExists{fontawesome5.sty}{\usepackage{fontawesome5}}{%
  \IfFileExists{fontawesome.sty}{\usepackage{fontawesome}}{%
    \providecommand{\faExclamationTriangle}{\textbf{!}}%
    \providecommand{\faLightbulb}{\textbf{$\ast$}}%
  }%
}

\clubpenalty=10000
\widowpenalty=10000
\displaywidowpenalty=10000

\emergencystretch=2em
% ---------------------------------------------------------------------
%  Idioma español
%  Se usa babel moderno con \babelprovide para no depender del antiguo
%  spanish.ldf. Aporta «Capítulo», «Índice», fechas y guiones en español.
% ---------------------------------------------------------------------
\usepackage{babel}
\babelprovide[import, main]{spanish}

% ---------------------------------------------------------------------
%  Geometría de página
% ---------------------------------------------------------------------
\usepackage[
  a5paper,
  top=2.0cm,
  bottom=2.0cm,
  inner=2.2cm,   % margen de lomo: no bajar de ~2cm en encuadernación
  outer=1.7cm,
  headsep=12pt,
  footskip=1.0cm,
]{geometry}

% ---------------------------------------------------------------------
%  Color: paleta del manual
% ---------------------------------------------------------------------
\usepackage{xcolor}
\definecolor{primario}{HTML}{1F4E5F}   % Azul petróleo (títulos, filetes)
\definecolor{secundario}{HTML}{8E3B46} % Granate (acentos)
\definecolor{aviso}{HTML}{B23A48}      % Rojo aviso
\definecolor{consejo}{HTML}{2E7D5B}    % Verde consejo
\definecolor{regla}{HTML}{B07D2B}      % Ámbar regla de oro
\definecolor{grisclaro}{HTML}{F4F4F1}  % Fondo suave de cajas

% ---------------------------------------------------------------------
%  Listas, tablas y figuras
% ---------------------------------------------------------------------
\usepackage{enumitem}         % Control fino de listas
\setlist{itemsep=2pt, topsep=4pt, parsep=0pt}
\usepackage{booktabs}         % Filetes de tabla de calidad
\usepackage{tabularx}         % Tablas de ancho fijo
\usepackage{array}            % Mejoras en columnas
\usepackage{ragged2e}         % \RaggedRight en celdas
\usepackage{graphicx}         % Imágenes (carpeta figures/)
\setkeys{Gin}{
    width=.76\textwidth,
    keepaspectratio
}
\graphicspath{{figures/}}

\usepackage[font=small,
labelfont=bf,
labelsep=period]{caption}

\captionsetup{
justification=centering,
skip=8pt
}

\setkeys{Gin}{
width=.82\textwidth,
keepaspectratio
}

% ---------------------------------------------------------------------
%  Comillas (estilo español: «...»)
% ---------------------------------------------------------------------
\usepackage{csquotes}

% ---------------------------------------------------------------------
%  Cajas destacadas (avisos, consejos, definiciones)
% ---------------------------------------------------------------------
\usepackage[most]{tcolorbox}

% Caja genérica con título e icono textual
\newtcolorbox{cajadestacada}[2][]{%
  enhanced, breakable,
  colback=grisclaro, colframe=#2,
  boxrule=0.4pt, left=10pt, right=10pt, top=8pt, bottom=8pt,
  borderline west={2pt}{0pt}{#2},
  arc=2pt, fonttitle=\bfseries\color{#2},
  #1
}

% Aviso importante (rojo)
\newtcolorbox{aviso}{%
  title=\faExclamationTriangle\ Aviso,
  enhanced, breakable,
  colback=aviso!6, colframe=aviso,
  boxrule=0.4pt, left=10pt, right=10pt, top=8pt, bottom=8pt,
  borderline west={2pt}{0pt}{aviso}, arc=2pt,
}

% Consejo (verde)
\newtcolorbox{consejo}{%
  title=\faLightbulb\ Consejo,
  enhanced, breakable,
  colback=consejo!6, colframe=consejo,
  boxrule=0.4pt, left=10pt, right=10pt, top=8pt, bottom=8pt,
  borderline west={2pt}{0pt}{consejo}, arc=2pt,
}

% Regla de oro (ámbar)
\newtcolorbox{reglaoro}{%
  title=Regla de Oro,
  enhanced, breakable,
  colback=regla!8, colframe=regla,
  boxrule=0.4pt, left=10pt, right=10pt, top=8pt, bottom=8pt,
  borderline west={2pt}{0pt}{regla}, arc=2pt,
}

% Definición / concepto (azul primario)
\newtcolorbox{definicion}{%
  title=Definición,
  enhanced, breakable,
  colback=primario!5, colframe=primario,
  boxrule=0.4pt, left=10pt, right=10pt, top=8pt, bottom=8pt,
  borderline west={2pt}{0pt}{primario}, arc=2pt,
}

% ---------------------------------------------------------------------
%  Estilo de títulos
% ---------------------------------------------------------------------
\usepackage{titlesec}

% --- Capítulos: número grande y ligero, título amplio y filete fino --------
\titleformat{\chapter}[display]
  {\normalfont\sffamily\color{primario}}
  {\filright
   {\color{secundario}\sffamily\bfseries\fontsize{12}{12}\selectfont
    \MakeUppercase{\chaptertitlename}}\;
   {\color{primario!35}\fontsize{44}{44}\selectfont\thechapter}}
  {4pt}
  {\LARGE\bfseries}
  [\vspace{6pt}{\color{primario!45}\titlerule[1.0pt]}]
\titlespacing*{\chapter}{0pt}{16pt}{18pt}

% Capítulos sin numeración (índice, bibliografía): sobrio y en línea
\titleformat{name=\chapter,numberless}[display]
  {\normalfont\sffamily\color{primario}}
  {}{0pt}
  {\LARGE\bfseries}
  [\vspace{6pt}{\color{primario!45}\titlerule[1.0pt]}]

\titleformat{\section}
  {\normalfont\sffamily\large\bfseries\color{primario}}
  {\thesection}{0.8em}{}
\titleformat{\subsection}
  {\normalfont\sffamily\bfseries\color{secundario}}
  {\thesubsection}{0.6em}{}

\titlespacing*{\section}{0pt}{2.2ex plus 1ex minus .2ex}{1.0ex plus .2ex}
\titlespacing*{\subsection}{0pt}{1.8ex plus .8ex minus .2ex}{0.8ex plus .2ex}

% --- Citas de apertura de capítulo (epígrafes) -----------------------------
\usepackage{epigraph}
\setlength{\epigraphwidth}{0.62\textwidth}
\renewcommand{\epigraphflush}{flushright}
\setlength{\epigraphrule}{0pt}
\setlength{\beforeepigraphskip}{0pt}
\setlength{\afterepigraphskip}{2.4\baselineskip}
% \citacap{texto de la cita}{autor}
\newcommand{\citacap}[2]{%
  \epigraph{\itshape #1}%
    {\normalfont\footnotesize\scshape\color{secundario}#2}}

% ---------------------------------------------------------------------
%  Encabezados y pies de página
% ---------------------------------------------------------------------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}

\renewcommand{\headrulewidth}{0.6pt}
\renewcommand{\footrulewidth}{0pt}

% Filete de cabecera con un tono suave del color primario
\renewcommand{\headrule}{%
  \hbox to\headwidth{\color{primario!35}%
    \leaders\hrule height \headrulewidth\hfill}}

\fancyhead[LE]{%
  \small\sffamily\scshape\color{primario}\nouppercase{\leftmark}}

\fancyhead[RO]{%
  \small\sffamily\scshape\color{primario}Defensa personal para mujeres}

\fancyfoot[C]{%
  \small\sffamily\color{primario!85}\thepage}

\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[C]{\small\sffamily\color{primario!85}\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

% ---------------------------------------------------------------------
%  Espaciado e interlineado
% ---------------------------------------------------------------------
\usepackage{setspace}
\setstretch{1.20}
\setlength{\parskip}{0.60em plus 0.15em}
\setlength{\parindent}{0pt}
\usepackage{emptypage}        % Páginas en blanco realmente en blanco

% ---------------------------------------------------------------------
%  Bibliografía (autor-año con natbib + BibTeX)
% ---------------------------------------------------------------------
\usepackage[round, authoryear]{natbib}
\bibliographystyle{plainnat}
\setcitestyle{aysep={,}}

% ---------------------------------------------------------------------
%  Hiperenlaces y marcadores (cargar casi al final)
% ---------------------------------------------------------------------
\usepackage[
  colorlinks=true,
  linkcolor=primario,
  citecolor=secundario,
  urlcolor=secundario,
  pdftitle={Defensa Personal para Mujeres. Manual del Estudiante},
  pdfauthor={Jorge Aranda Moro - Artes Marciales Bilbao},
  pdfsubject={Manual de defensa personal y autoprotección},
]{hyperref}
\usepackage{bookmark}
\urlstyle{same}

% ---------------------------------------------------------------------
%  Comandos auxiliares
% ---------------------------------------------------------------------
% Teléfono/recurso destacado en línea
\newcommand{\recurso}[1]{\textbf{\color{primario}#1}}

\usepackage{eso-pic}

% ---------------------------------------------------------------------
%  Indice
% ---------------------------------------------------------------------

\usepackage{tocloft}

\renewcommand{\cftchapfont}{\sffamily\large\bfseries\color{primario}}
\renewcommand{\cftchappagefont}{\sffamily\large\bfseries\color{primario}}
\renewcommand{\cftsecfont}{\sffamily}
\renewcommand{\cftsecpagefont}{\sffamily}
\renewcommand{\cftsecleader}{\normalfont\cftdotfill{\cftsecdotsep}}

\setlength{\cftbeforechapskip}{12pt}
\setlength{\cftbeforesecskip}{2pt}

% Título del índice en la línea del diseño del resto del libro
\renewcommand{\cfttoctitlefont}{\normalfont\sffamily\LARGE\bfseries\color{primario}}
\setlength{\cftaftertoctitleskip}{14pt}

% ---------------------------------------------------------------------
%  Páginas de relleno para cuadrar el pliego de imprenta
% ---------------------------------------------------------------------
% Página de cortesía: completamente en blanco, sin folio ni cabecera.
\newcommand{\paginacortesia}{%
  \clearpage
  \thispagestyle{empty}%
  \null
  \clearpage}

% Página de notas: rayado suave para la alumna. Más elegante que un
% blanco puro para rellenar el final de un manual de entrenamiento.
% (\foreach está disponible porque tcolorbox[most] carga tikz/pgffor.)
\newcommand{\paginanotas}{%
  \clearpage
  \thispagestyle{plain}%
  \null
  {\noindent\sffamily\Large\bfseries\color{primario}Notas\par}%
  \vspace{1.2em}%
  \begingroup
    \setstretch{1}%
    \foreach \n in {1,...,14}{%
      \noindent{\color{primario!25}\rule{\linewidth}{0.4pt}}\par
      \vspace{1.7em}}%
  \endgroup
  \clearpage}